```instructions
# Frontend - LLM Instructions

Summary:
- Frontend is a GUI tooling project that relies on Core and SDL2.
- It ships ImGui sources and a local `cmake/ThirdParty.cmake` for SDL2.

Guidance for LLMs:
- Keep dependency fetching localized to this project.
- Do not change Core portability assumptions.
- Prefer documentation and build helper improvements over large refactors.

```