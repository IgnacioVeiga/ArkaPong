#include "InputManager.h"

void InputManager::handleInput(FlowManager &flowManager)
{
    // const Uint8 *state = SDL_GetKeyboardState(NULL);

    // if (flowManager.getCurrentState() == GameStatus::START_MENU && state[SDL_SCANCODE_SPACE])
    // {
    //     flowManager.changeState(GameStatus::START_COUNTDOWN);
    // }
    // else if (flowManager.getCurrentState() == GameStatus::PAUSED && state[SDL_SCANCODE_P])
    // {
    //     flowManager.changeState(GameStatus::PLAYING);
    // }
    // else if (flowManager.getCurrentState() == GameStatus::PLAYING && state[SDL_SCANCODE_P])
    // {
    //     flowManager.changeState(GameStatus::PAUSED);
    // }
}
